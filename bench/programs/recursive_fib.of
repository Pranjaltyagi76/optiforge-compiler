// Call-dominated: the function-entry counter is one increment per call, against
// a body that does almost nothing.
fn fib(int n) -> int {
    if (n < 2) { return n; }
    return fib(n - 1) + fib(n - 2);
}
fn main() -> int { print_int(fib(38)); return 0; }
