// Pins the lowering of `for`, and of `continue` reaching the step clause.
fn main() -> int {
    int total = 0;
    for (int i = 0; i < 6; i = i + 1) {
        if (i == 2) { continue; }
        if (i == 5) { break; }
        total = total + i;
    }
    print_int(total);
    return 0;
}
