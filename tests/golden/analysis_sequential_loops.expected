=== exit ===
0
=== stdout ===
function @twice
  dominator tree:
    entry
      while.cond.1
        while.body.2
        while.end.3
          while.cond.4
            while.body.5
            while.end.6
  post-dominator tree:
    <exit>
      while.end.6
        while.cond.4
          while.end.3
            while.cond.1
              entry
              while.body.2
          while.body.5
  dominance frontiers:
    entry: -
    while.cond.1: while.cond.1
    while.body.2: while.cond.1
    while.end.3: -
    while.cond.4: while.cond.4
    while.body.5: while.cond.4
    while.end.6: -
  loops:
    loop header=while.cond.1 depth=1
      latches:   while.body.2
      blocks:    while.cond.1, while.body.2
      exits:     while.end.3
      preheader: entry
    loop header=while.cond.4 depth=1
      latches:   while.body.5
      blocks:    while.cond.4, while.body.5
      exits:     while.end.6
      preheader: while.end.3
  liveness:
    entry:
      in:  %n
      out: %n.addr
    while.cond.1:
      in:  %n.addr
      out: %n.addr
    while.body.2:
      in:  %n.addr
      out: %n.addr
    while.end.3:
      in:  %n.addr
      out: %n.addr
    while.cond.4:
      in:  %n.addr
      out: %n.addr
    while.body.5:
      in:  %n.addr
      out: %n.addr
    while.end.6:
      in:  %n.addr
      out: -
  reaching definitions (stores):
    entry: in=-  out=%n.addr@entry
    while.cond.1: in=%n.addr@entry, %n.addr@while.body.2  out=%n.addr@entry, %n.addr@while.body.2
    while.body.2: in=%n.addr@entry, %n.addr@while.body.2  out=%n.addr@while.body.2
    while.end.3: in=%n.addr@entry, %n.addr@while.body.2  out=%n.addr@entry, %n.addr@while.body.2
    while.cond.4: in=%n.addr@entry, %n.addr@while.body.2, %n.addr@while.body.5  out=%n.addr@entry, %n.addr@while.body.2, %n.addr@while.body.5
    while.body.5: in=%n.addr@entry, %n.addr@while.body.2, %n.addr@while.body.5  out=%n.addr@while.body.5
    while.end.6: in=%n.addr@entry, %n.addr@while.body.2, %n.addr@while.body.5  out=%n.addr@entry, %n.addr@while.body.2, %n.addr@while.body.5

function @main
  dominator tree:
    entry
  post-dominator tree:
    <exit>
      entry
  dominance frontiers:
    entry: -
  loops:
    none
  liveness:
    entry:
      in:  -
      out: -
  reaching definitions (stores):
    entry: in=-  out=-

=== stderr ===
